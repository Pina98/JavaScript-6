let i = 0
let num = 0
let media = 0

do {
    num = parseFloat(prompt('Digite o Números (Digite 0 para finalizar):'));

    if(num % 3 == 0){
        media = media + num
        i++
    }
    

} while (num != 0);

const total = media / i

document.write(`O Total é: ${media}`)